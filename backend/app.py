from flask import Flask

app = Flask(__name__)

@app.route("/")


#list of endpoints

#login - fetching users data/password combo / authenticate to the database

#receive query from user and response in the same endpoint (through http obviously)

#refresh

#click individal buttons - fetch (resource related to that document with that buttons document ID

